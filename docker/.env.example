# Relative path to the project root that should be mounted into the containers
PROJECT_ROOT=../
BACKEND_DIR=backend
FRONTEND_DIR=frontend

# Networking
NGINX_PORT=8080
VITE_PORT=5173
PHP_FPM_PORT=9000

# Host integration
HOST_UID=1000
HOST_GID=1000
HOST_TZ=Europe/Moscow
HOST_LOCALE=ru_RU.UTF-8

# Git identity propagated into the containers
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=you@example.com
